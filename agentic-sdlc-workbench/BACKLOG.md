# Agentic SDLC Workbench — Backlog

> Items are logged in rough discovery order. Update status as work progresses.
> Types: **Feature** | **Defect**

---

## ✅ Completed

| # | Type | Description |
|---|------|-------------|
| #3 | Feature | Cost scoping — per-Application planning/entitlement fields; global pricing stays on rate card |
| #5 | Feature | Draft Agent Prompt via Claude — AI-assisted system prompt drafting with stub fallback |
| #8 | Feature | Automated requirements → test cases traceability |
| #9 | Feature | Design Repository Quality Reviewer + CP Audit Trail — on-demand AI auditor, findings panel, CP history panel |
| #59 | Defect | **ServiceNow round-trip sys_id write-back** — added `POST /projects/:id/servicenow/register-sysid` so a Workbench-authored entity's returned ServiceNow `sys_id` is recorded on its row; the next delta export then classifies it as a PATCH (not POST), preventing duplicate records on the 2nd pass. Export deployment instructions include the call. ([server.js](agentic-sdlc-workbench/backend-node/server.js)) _(critical-AI-capabilities review 2026-06-11.)_ |
| #60 | Defect | **Net-new requirement conflict detection** — cross-check now scans brand-new requirements against existing ones and raises a **blocking** conflict clarification on a genuine contradiction/duplication; previously only update/delete were checked, so a new contradicting requirement was staged silently. ([cross-check.js](agentic-sdlc-workbench/backend-node/agent/cross-check.js)) _(critical-AI-capabilities review 2026-06-11.)_ |
| #61 | Defect | **Real baseline snapshots + compare** — locking a baseline (and every release) now snapshots the full design into `asdlc_baseline_item`, and compare diffs snapshots (added/removed/modified records & fields) instead of always-zero counters — real versioning + a rollback basis. ([server.js](agentic-sdlc-workbench/backend-node/server.js)) _(critical-AI-capabilities review 2026-06-11.)_ |
| #62 | Defect | **✅ FIXED 2026-07-03 — ServiceNow-conflict approval silently dropped the accepted value.** Reproduction showed the drop was wider than first reported: whole-CP Approve AND per-item accept-as-is both wrote nothing for `_sn_proposed` fields (only per-item approve WITH explicit `field_overrides` worked). Fix: `promoteSnProposed()` ([server.js](agentic-sdlc-workbench/backend-node/server.js)) promotes remaining SN-proposed values into real fields on approval, persists the merged `new_value` + decision note on the item, and force-applies through the shrink guard (same exception as reviewer overrides) in BOTH apply paths. Guard unchanged for non-ratified fields. Regression test: `test-cp-conflict-apply.js` (15). _(Round-trip fidelity assessment 2026-07-03.)_ |
| #88 | Feature | **✅ DONE 2026-07-06 — Baseline restore endpoint.** `POST /baselines/:id/restore` (`?dry_run=1` plan-only) diffs the locked snapshot vs the live design and creates a reviewable Change Packet of `.restore` items — materialized column-verbatim by `mtRestore()` ([server.js](agentic-sdlc-workbench/backend-node/server.js); no transform double-wrap, provenance/identity never restored, audited as RESTORE). Nothing applies until a human approves; missing-since-baseline records reported (never recreated), added-since reported (never retired). Regression test: `test-baseline-restore.js` (17). _(Round-trip fidelity assessment, gap G8/R8.)_ |
| #85 | Feature | **✅ DONE 2026-07-06 — Hash-advance refreshes the last-synced SN snapshot.** The changed→no_change path ([server.js](agentic-sdlc-workbench/backend-node/server.js) sync step 3) now refreshes `source_fluent` (Tier-3 "as built": script body for logic, salient JSON otherwise; COALESCE never blanks it) and keeps the generic twin's `source_hash`/`source_fluent` in lockstep by sys_id — the hash can no longer claim a currency the snapshot doesn't have, giving the R7 3-way merge a faithful base. Regression test: `test-snapshot-refresh.js` (11). _(Round-trip fidelity assessment, gap G5/R4.)_ |
| #87 | Feature | **✅ DONE 2026-07-06 — Deploy identity keys manifest in both exports.** SN Delta + full Build Spec emit "🔑 Deploy Identity — keys.ts Seed Manifest": stable Now.ID keys (`wb-<project8>-<slug>`), table + known sys_id per synced record, human table + machine-readable JSON, with the key-stability invariants (seed before first build; never rename a deployed key; create-path rule + register-back). Closes the Now.ID-mints-fresh-sys_ids duplicate loop; strengthens #70's manual handoff. Regression test: `test-keys-manifest.js` (10). _(Round-trip fidelity assessment, gap G6/R6.)_ |
| #84 | Feature | **✅ DONE 2026-07-04 — Deterministic both-side-edit detection for the SN sync.** `classifyArtifacts` now stamps `changed` items with `wb_edited_since_sync` (`updated_at` vs `sn_last_synced_at`); the gate ([sn-sync.js](agentic-sdlc-workbench/backend-node/agent/sn-sync.js)) NEVER auto-applies a both-side-changed item — rich (even approved fill_blank) or generic (auto-refresh now respects Phase-4b Workbench edits); `editContextLines()` states the fact in the reconciler/reviewer prompts and the plan view/CP rationale carry it to the human. Kills the deliberately-cleared-field resurrection hazard. Remaining half of gap G2 (field-level 3-way merge from a stored base) stays open as the R7 recommendation — needs #85 first. Regression test: `test-both-side-edit.js` (21). _(Round-trip fidelity assessment, gap G2/R3.)_ |
| #98 | Feature | **✅ DONE 2026-07-07 — Slice-scoped ingest: per-project editable import profile.** Bounds an ingest to a SUBSET of a scope. New additive `asdlc_project.sn_import_profile_json` ([db.js](agentic-sdlc-workbench/backend-node/db.js) migration; no existing table altered) + GET/PUT `/projects/:id/servicenow/import-profile` ([server.js](agentic-sdlc-workbench/backend-node/server.js); seeds from the latest assessment's `recommended_profile`). `captureScope`/`sweepScopeMetadata`/`classifyArtifacts` ([sn-capture.js](agentic-sdlc-workbench/backend-node/agent/sn-capture.js)) accept a `slice`: a surface allowlist (+ child tables of included parents), an optional per-surface cap, and a **reserved** `record_filters` for a future record-level filter (surface-only today). Drift candidacy AND the `sys_metadata` sweep are bounded to the same slice, so out-of-slice rows are never read as deleted. Assessment UI makes census surfaces selectable (Save/Reset) ([servicenow_assessment.js](agentic-sdlc-workbench/frontend/modules/servicenow_assessment.js)); Sync UI shows the active slice ([servicenow_sync.js](agentic-sdlc-workbench/frontend/modules/servicenow_sync.js)); write-back is unchanged (per-entity delta). Regression test: `test-slice-ingest.js` (22). _(User request 2026-07-07.)_ |
| #99 | Feature | **✅ DONE 2026-07-07 — R7 field-level two-way merge (the remaining half of gap G2).** New PURE [three-way-merge.js](agentic-sdlc-workbench/backend-node/agent/three-way-merge.js) (`classifyFields`/`snFieldDelta`/`mergeSafe`) buckets each field, base-vs-WB-vs-SN, into unchanged/sn_only/wb_only/both_changed. **Generic artifacts** (whose Workbench payload IS the ServiceNow payload → a sound 3-way) get a true field-level merge in `buildGenericPlan` ([sn-sync.js](agentic-sdlc-workbench/backend-node/agent/sn-sync.js)): a both-side-edited generic no longer parks the whole record in HITL — disjoint edits auto-apply a SAFE merge (apply `sn_only`, keep every Workbench edit incl. a deliberate clear — the fill_blank-resurrection fix, now per field), and only a genuine `both_changed` field goes to HITL naming the field. **Rich artifacts** get the deterministic SN-side field delta injected into the reconciler prompt ([sn-reconcile.js](agentic-sdlc-workbench/backend-node/agent/sn-reconcile.js) via `editContextLines`); the record-level both-side floor stays as the safe fallback. Builds on #84/#85/#86. **Known scope:** rich base is SN-field-space (`source_fluent`); a full WB-field-space merge for rich would need a stored WB-side base snapshot (documented follow-on, see #100). Tests: `test-three-way-merge.js` (24) + generic-merge cases in `test-slice-ingest.js`. _(Round-trip fidelity assessment, gap G2/R7.)_ |
| #86 | Feature | **✅ DONE 2026-07-06 — `sys_metadata` completeness + change backbone (both parts).** **(a)** `sweepScopeMetadata()` + pure `analyzeCompleteness()` ([sn-capture.js](agentic-sdlc-workbench/backend-node/agent/sn-capture.js)) run ONE read-only scope sweep (the authority for all ~40 classes) → reports blind-spot classes the ~24 curated surfaces never read (per-class counts + samples) and splits WB-side drift into `vanished` (absent from the sweep ⇒ deleted upstream) vs `present_uncaptured` (exists under an unmonitored class); attached as `plan.completeness` + warnings, best-effort (unreadable sweep ⇒ `available:false`, never breaks sync). Test: `test-sn-completeness.js` (29). **(b)** New `asdlc_sn_change_signal` table (standalone `CREATE TABLE IF NOT EXISTS` — no existing table altered) stores each record's `sys_mod_count`/`sys_updated_on`/`sys_updated_by` on every successful sync; next sync, a hash-changed record whose counter is UNMOVED is flagged `sn_unmoved` and routed to a deterministic no_change (hash refreshed, **no Opus**) — killing the mass-reclassify a salient-formula change would otherwise trigger — and who/when flows into the reconciler prompt + CP rationale. Test: `test-sn-change-signal.js` (23). Gate/auto-apply matrix unchanged. _(Round-trip fidelity assessment 2026-07-03, gaps G3+G4/R5; probe log in the assessment doc.)_ |
| — | Feature | **✅ DONE 2026-07-03 — AI model/effort configuration completed (registry-as-data).** New [model-registry.json](agentic-sdlc-workbench/backend-node/agent/model-registry.json) (incl. `claude-fable-5`, `claude-sonnet-5`) + `model_registry_custom` runtime overlay; all 11 roles settable (model + `_thinking_effort` off/low/medium/high/xhigh/max) via API/env/Admin UI; boot validation w/ loud fallback; pricing decoupled (unpriced models badge instead of silent NULL); fixed the never-working admin effort dropdown and the invalid thinking shape on Haiku-backed roles. `test-ai-config.js` (51). _(Round-trip fidelity assessment 2026-07-03.)_ |

---

## 🐛 Open Defects

| # | Description |
|---|-------------|
| #2 | Fix logon/auth flow |
| #4 | Fix report formatting for Tools, Agents, Workflows, Use Cases |
| #17 | CP action buttons (Approve / Reject / Send Back) remain active after a terminal action — should be greyed out |
| #18 | Cost projections not updated after a CP approval adds or modifies Assist-consuming entities (Agents, Workflows, Use Cases) |
| #36 | **RASIC not being populated — RECURRING regression.** Workflow/use-case RASIC matrices come through empty again after ingest. Previously addressed (AI RASIC inference + swimlane/design-repair pipeline, commit `750e18d`; deriver in [rasic-deriver.js](agentic-sdlc-workbench/backend-node/agent/rasic-deriver.js)) but has regressed. Because it keeps coming back, the fix must include a **durable guard**: (a) find the root cause of *this* recurrence (is `rasic-deriver` being invoked in the current ingest/synthesis path? did the two-tier Sonnet+Opus pipeline change bypass it? are participants/roles present for it to assign against?); (b) add a **post-ingest assertion / smoke check** that flags when a materialized workflow has participants but an empty RASIC, so the regression is caught automatically instead of by eye. Relates to the orphan-reqs/swimlane derivation fix. |
| ~~#22~~ | ✅ Resolved — two-layer fix: (1) extraction prompt now instructs the AI Agent to scan for use cases BEFORE extracting FRs/NFRs so it can set `use_case_title` correctly; (2) at promote time, any still-orphan FRs/NFRs are passed to a Haiku AI Agent (`req-linker.js`) that infers the parent use case and updates `entity_data` before the CP items are written (non-fatal). |
| #37 | **Non-transactional multi-writes in core flows (data-corruption risk).** The ingest **promote** handler ([server.js:6943](agentic-sdlc-workbench/backend-node/server.js)) writes the CP header, loops `insertItem.run()` + `markPromoted.run()` per extraction, then updates the doc — with no transaction; the ServiceNow **sync** write-back ([server.js:7201](agentic-sdlc-workbench/backend-node/server.js)) commits the HITL packet, auto packet + `approveAndApplyCp`, `source_hash` advancement, and `markSynced` independently. A mid-loop failure half-commits and corrupts ingest/sync state. Wrap each in `BEGIN`/`COMMIT` with `ROLLBACK` (the change-packet apply flows already do this). _(From backend review 2026-06-11.)_ |
| #38 | **No Express error-handling middleware.** [server.js](agentic-sdlc-workbench/backend-node/server.js) has only 404 catch-alls; ~30+ handlers run `db.prepare().get()/run()` with no try/catch, so an uncaught SQLite error (FK/CHECK/UNIQUE, bad JSON body) or rejected async promise auto-forwards to Express 5's default handler → bare 500, no JSON body, UI sees an opaque failure. Add a terminal `app.use((err,req,res,next)=>res.status(err.status||500).json({error:err.message}))` after all routes. _(From backend review 2026-06-11.)_ |
| #39 | **SQL injection vector in `/library`.** The endpoint builds SQL by string-interpolating the `scope` query param with manual `replace(/'/g,"''")` escaping ([server.js:4002](agentic-sdlc-workbench/backend-node/server.js), 4 sites) — the only non-parameterized query in server.js. Bind it with `?` like every other list endpoint. _(From backend review 2026-06-11.)_ |
| #40 | **Clarification auto-apply overwrites cost data project-wide.** [server.js:6691](agentic-sdlc-workbench/backend-node/server.js) rewrites `runs_per_period` on **every** workflow (`WHERE project_id=? AND (runs_per_period IS NULL OR =0)`) and `cost_model` on every agent from one free-text answer parsed with `/\d+/` — one document's clarification silently skews all cost projections. Scope the write to workflows/agents linked to the originating ingest, or surface the parsed value for confirmation. _(From backend review 2026-06-11.)_ |
| #41 | **ServiceNow sync leaks raw error detail to the client.** The `sync` endpoint returns raw `err.message` from the live SN REST pull in its 500/502 bodies ([server.js:7142](agentic-sdlc-workbench/backend-node/server.js)), which can surface instance URLs / credential-hint text. Scrub or generalize the message; log the detail server-side only. _(From backend review 2026-06-11.)_ |
| #42 | **Migration loop swallows all errors.** [db.js:298](agentic-sdlc-workbench/backend-node/db.js) wraps the `MIGRATIONS` loop in `catch {}`, so a genuinely broken `ALTER`/`CREATE TABLE` is hidden as if it were a benign duplicate-column; the array also runs a data `UPDATE` (`cp_origin` backfill) whose failure is invisible. Only swallow when `err.message` matches "duplicate column"/"already exists"; log everything else; move data `UPDATE`s to a logged path. _(From backend review 2026-06-11.)_ |
| #43 | **`/cost-estimate` Opus call is invisible and uncapped.** [server.js:3270](agentic-sdlc-workbench/backend-node/server.js) hardcodes `model:'claude-opus-4-5'` + `max_tokens:4096` instead of `aiConfig.resolveModel()/getMaxTokens()`, and never writes to `asdlc_ai_usage` — so this Opus call doesn't appear in the `/usage` dashboards and isn't bound by the admin model setting. Route it through `aiConfig` and log `message.usage`. Relates to #21. _(From backend review 2026-06-11.)_ |
| #49 | **Baseline detail/compare fetch uses the wrong id field.** [baseline.js:208](agentic-sdlc-workbench/frontend/modules/baseline.js) fetches `/baselines/${baseline.id}`, but the list API populates `baseline_id` (used everywhere else); `baseline.id` is `undefined`, so the fetch hits `/baselines/undefined`, fails, and the `.catch(() => baseline)` silently falls back to the stale list row — the detail/compare panes never show fresh server data. Change to `${baseline.baseline_id}`. _(From frontend review 2026-06-11.)_ |
| #50 | **Ingest UI discards the `standing_applied` feedback.** The clarifications/answer POST throws away its response ([ingest.js:920](agentic-sdlc-workbench/frontend/modules/ingest.js)), but the backend now returns `standing_applied` summarizing the project-wide standing-question fills (the #40 fix) — so the user gets no feedback that answering a clarification just rewrote `runs_per_period`/`cost_model` across the project. Capture the result and surface it (toast or summary) when `standing_applied?.length`. Relates to #40 and #48. _(From frontend review 2026-06-11.)_ |
| #51 | **cost_management edit-skill modal renders unstyled.** [cost_management.js:135](agentic-sdlc-workbench/frontend/modules/cost_management.js) uses class names `_p2Overlay`/`_p2Header`/`_p2Close`, which are **JS helper functions in design_review.js**, not CSS classes — so the modal has no overlay backdrop, no centering, and an unstyled close button (appears as a raw block at page bottom). Use real modal classes (e.g. `modal-overlay`/`modal-card` as in library.js) or the shared modal helper. Root cause is the missing shared modal helper (#56). _(From frontend review 2026-06-11.)_ |
| #52 | **Stub buttons fake success.** Two actions report success when nothing happened: evidence "Re-run Extraction" ([evidence.js:186](agentic-sdlc-workbench/frontend/modules/evidence.js)) `await`s an 800ms timer then toasts "Extraction queued successfully"; reports "Preview" ([reports.js:158](agentic-sdlc-workbench/frontend/modules/reports.js)) toasts "Preview generated (stub)". Wire to real endpoints or disable/label as not-implemented. Evidence is already tracked as #20; this adds the reports Preview and the deceptive-success framing. _(From frontend review 2026-06-11.)_ |
| #53 | **A module render() throw leaves the SPA in a bad state.** [app.js:64](agentic-sdlc-workbench/frontend/app.js) `navigate()` catches the throw and shows an error state but doesn't reset `currentModule`, so the failed module stays active and is re-invoked on a project-selector change; there's also no global `unhandledrejection` handler, so post-render async throws surface only in the console. Clear `currentModule` on error and add a top-level rejection logger. _(From frontend review 2026-06-11.)_ |
| #54 | **testing.js assumes `/projects` is a bare array.** [testing.js:59](agentic-sdlc-workbench/frontend/modules/testing.js) does `projects.forEach` while every other module handles both `[]` and `{items:[]}`; if that endpoint returns the paginated shape the project dropdown silently empties and the tab is unusable. Normalize with `Array.isArray(data) ? data : (data.items || [])`. _(From frontend review 2026-06-11.)_ |
| #63 | **Cross-document duplicate entities (mtCreate has no identity match).** A `create` item never checks for an existing same-identity row ([server.js](agentic-sdlc-workbench/backend-node/server.js) `mtCreate`); a second document describing the same workflow/agent/tool inserts a duplicate unless the model happens to tag it `update` with a correct `target_slug`. Add a deterministic identity match (normalized name/slug within the project) that converts to an update or raises a conflict. _(From critical-AI-capabilities review 2026-06-11.)_ |
| #90 | **Dead Python FastAPI/PostgreSQL backend scaffold + stale README.** [backend/](agentic-sdlc-workbench/backend) (20 files: `main.py`, `database.py`, `deps.py`, 10 routers, 4 SQL migrations) is tracked in git from the single "Initial backup" commit and hasn't been touched since — it predates the pivot to [backend-node/](agentic-sdlc-workbench/backend-node) (the real, actively-developed, 189-route backend). Zero code anywhere references it, but root `README.md` still documents it as if it were live, which will send a new developer down a dead path exactly when `docs/KT_ONBOARDING.md` is trying to orient them correctly. Delete `backend/` and fix `README.md`; add a `backend-node/README.md` since the real backend has no scoped orientation doc of its own. _(Tech-debt audit 2026-07-06.)_ |
| #91 | **`multer` HIGH + `qs` MODERATE known vulnerabilities (`npm audit`).** `multer` 2.1.1 has GHSA-72gw-mp4g-v24j (DoS via deeply nested field names, CVSS 7.5) — this project's own file-upload path is exposed; a transitive `qs` has GHSA-q8mj-m7cp-5q26 (DoS via crafted array input). Both have fixes already available in the dependency tree (`multer` 2.2.0) — this is a version bump, not a migration. Also stale: `@anthropic-ai/sdk` (15 minors behind), `openai` (8 minors behind), `docx` (1 minor behind). _(Tech-debt audit 2026-07-06.)_ |
| #64 | **PDF ingest produces confident garbage instead of an error.** [document-reader.js:37](agentic-sdlc-workbench/backend-node/agent/document-reader.js) handles DOCX/TXT/CSV/audio; a `.pdf` falls through to `readFileSync(..,'utf8')`, sending binary noise to Claude that gets "extracted" into a hollow-but-plausible design. Add a PDF reader (e.g. pdf-parse) and make unknown/binary types set `ingest_status='failed'` instead of producing junk. _(From critical-AI-capabilities review 2026-06-11.)_ |

---

## 🚀 Open Features

| # | Description |
|---|-------------|
| #79 | **🔴 HIGH PRIORITY — Integration test: SN Generic Artifacts (Phases 1–3, Dev branch).** The following scenarios cannot be verified via the workbench UI and require a live ServiceNow connection or direct DB access. Must be completed before Dev is merged to main: (1) **Schema** — confirm `asdlc_sn_artifact` and `asdlc_sn_type_registry` tables + all indexes exist post-migration; (2) **SN capture** — run capture on a connected project, verify generic artifacts written with correct `tier`, `sn_metadata_type`, `source_sys_id`, `source_hash`; (3) **Tier-A twins** — confirm L1 rows (`asdlc_data_model` etc.) have `sn_artifact_id` back-linked after capture; (4) **Tier-B/C** — artifacts with no L1 table mapping are stored as generic rows (not silently dropped); (5) **Recursive children** — child rows have `parent_artifact_id` populated (e.g. Table→Column, CatalogItem→Variable); (6) **Delta export** — Build Spec with "Generic Artifacts (SN)" section selected emits deployable Fluent; preview card shows `Generic SN Artifacts  (A:x  B:y  C:z)` counts; (7) **Round-trip identity** — re-capturing unchanged artifacts produces identical `source_hash` / `source_sys_id` (no phantom diffs); (8) **Capability sync** — `sn-capability-sync.js` completes without error on a project with generic artifacts; (9) **Build Spec round-trip identity** — export → import cycle doesn't alter the artifact registry. _(From branch health audit 2026-06-23; Dev branch commits b0f09a7–354eeb4–e340a1c–5df4cca–13015c4.)_ |
| #1 | Source doc links — save traceability links from source documents to design artifacts |
| #6 | Show cost estimating logic in the UI — transparency on how Assist counts are calculated |
| #7 | AI-summarised requirements from unstructured documents |
| #10 | Baselines & versioning — mark baseline snapshots, compare to current (deferred from #9; implementation shape TBD at planning time) |
| #11 | Client-level (cross-Application) document library — store and recall reference docs across Applications |
| #12 | Per-Application source-material inventory — track docs, recordings, transcripts tied to an Application |
| #13 | Requirement dedup / similarity check agent — flag near-duplicate or overlapping requirements |
| #14 | Recording + transcription agent — multi-speaker capture and transcription into the Design Repository |
| #15 | Conflict cross-pointers — bidirectional links between conflicting requirements |
| #16 | Model tiering — Opus for complex work (ingest, full-app audit, conflict detection), Sonnet for lighter tasks; single `model-policy.js` config |
| #19 | RAG best practices documents — better mechanism for ingesting and managing reference material (standards, guidelines, playbooks) for agent retrieval |
| #20 | Fix Supporting Evidence functionality — buttons currently do nothing useful; wire up to meaningful actions |
| #21 | Per-Application Claude/AI cost view — surface AI usage & spend at the Application (project) level, not only in the global Admin AI Settings; roll up `asdlc_ai_usage` by `project_id` so Opus-heavy work (e.g. ServiceNow reconciliation/extraction) is attributable to the right Application |
| #23 | Inference dials beyond materiality (fast-follow to the materiality gate) — pre-run cost estimate / dry-run projection, per-run budget ceiling, per-item *adaptive* reverse-engineer effort, cheap→Opus *escalation*, and Economy/Balanced/Maximum **Effort Profile** presets bundling model+thinking+materiality. Floor today: per-role config in `ai-config.js` + post-hoc `asdlc_ai_usage`. |
| #24 | Reverse-path "force-elevate" — let a human promote a previously *captured-not-elevated* ServiceNow logic artifact into a Level-1 `business_logic` design row (e.g. from the sync report), and re-capture its body on demand. |
| ~~#25~~ | ✅ Resolved — `catalog_item` is now an intentional forward-BRD extraction target: a customer-facing intake form is modelled as a Service Catalog record producer (variables = fields), distinct from `form_design` (internal record layout). Prompt clarifies the choice. |
| #26 | Propagate the **✨ AI-suggested** badge tool-wide wherever a `system_generated` entity appears — **majorly the Design Review section** (badge the materialized NFR/use_case/workflow/workflow_step/tool/data_source rows + a legend), and secondarily the Build Spec / reports. Reuse the badge from Change-Packet review. |
| #27 | **L2 provenance on forward-only types** — add the standard `source_*` (source_system/sys_id/table/scope/fluent/hash) columns to `asdlc_guardrail`, `asdlc_data_source`, `asdlc_hitl_gate` **if/when they round-trip from ServiceNow** (SN policy/guardrail, SN data source, SN approval). Deferred from the L1/L2 field review (decision #4); they're forward-only today so no L2 yet. FR/NFR/AC/TC/user_story stay L1-only by design. |
| #28 | **Curated exemplar designs (few-shot gold standards)** — maintain 3–10 "thin BRD → excellent finished design" pairs (FRs, UCs, workflows, steps, agents, tools, forms, data model, tests, RASIC) shown to the ingestion AI so it learns the target *shape and depth* of a good design. Highest-ROI ingestion-quality lever; feeds the Opus design-synthesis pass. _(Ingestion-quality program — see plan; training data #1.)_ |
| #29 | **Retrieval (RAG) over the approved-design corpus** — index every promoted design; at ingest, retrieve the most similar priors so output matches house style and improves as the repo grows. Distinct from #19 (external reference material) — this retrieves *our own finished designs*. _(Ingestion-quality program — training data #2.)_ |
| #30 | **Lessons-learned + anti-patterns flywheel** — capture clarification Q→A, accepted edits (proposed vs final), rejections, and quality-review fixes (`asdlc_ingest_feedback`); cluster recurring ones; auto-inject positives as best-practice rules and negatives as anti-pattern rules ("don't make a `catalog_item` for an internal form"). Automates the manual "Save as best practice" loop on the AI Guidance & Learning page. _(Ingestion-quality program — training data #3.)_ |
| #31 | **"Suggestion Level" slider on the ingestion pane** (1–99, default ~90) — temperature-style *aggressiveness* control (deliberately **not** "confidence") that replaces the enrichment dropdown + per-project confidence threshold. **High = AI aggressively proposes entities + element values; low = stays close to what the doc states** (stated content always extracted). Inverse polarity of a threshold; internal per-field confidence still drives the ✨ AI-suggested badge + 0%→blank rule. Implement as prompt posture + inverted acceptance bar (not LLM sampling temperature). Related to #23. **Deferred from Phase 1 as gold-plating; revisit in Phase 2** (per user 2026-06-09). Until then, both the AI Mode dropdown and the Agent Confidence Threshold field remain as the user-facing controls (AI Mode default = Balanced). |
| #32 | **Tune Opus synthesis cost (quick levers)** — the synthesis pass has no cost cap today. On a Claude-4 model the `synthesis_thinking_budget` is only an effort-tier selector (≥8000→high), not a token cap. (a) Lower it to the `medium` tier to reduce deliberation; and/or (b) give the synthesis pass its OWN loop cap (e.g. 5) instead of sharing `max_extraction_loops` (20). Both are small config/code changes in `agent/ai-config.js` + `agent/claude-processor.js`. Observed ≈ $4.1 per `suggestive` ingest at effort=high. |
| #33 | **Per-ingest AI cost/token budget guard (hard ceiling)** — a true spend ceiling for an ingest run (extraction + synthesis), tied to the Cost Management settings: stop/skip the Opus synthesis pass when a per-run token or USD budget is hit, surfaced in the ingest UI. Net-new work (the "cost ceiling" open-risk from the plan). Relates to #21 (per-Application AI spend view) and #32. |
| #34 | **Better ingest time expectation / progress feedback** — the ingest UI still tells users "this can take 1–3 minutes" ([ingest.js](agentic-sdlc-workbench/frontend/modules/ingest.js)), but the two-tier pipeline (Sonnet extraction + Opus synthesis pass) now runs longer (observed ~6 min on a small BRD; larger/`suggestive` docs more). Update the copy to a realistic range AND ideally show live progress — a phase indicator (Extracting → Synthesizing → Reviewing) and/or an elapsed timer driven by the existing status poll — so the user isn't left guessing. |
| #35 | **Dedicated cheap "clarifier" pass (owns question generation + semantic dedup)** — clarifying questions are currently raised from 3 places (Sonnet extraction auto-raise, the Opus synthesis pass, and deterministic standing questions), and `writeClarification` only dedupes on an exact `target_entity_type`+`target_field` match ([claude-processor.js:254](agentic-sdlc-workbench/backend-node/agent/claude-processor.js)) — so near-identical questions slip through (observed: the PO was shown duplicate questions). Fix: a single dedicated **`clarifier` role on a cheap model** (Haiku/Sonnet — reuse the `role` param on `runExtractionLoop` + the `ai-config` role pattern) that owns clarification generation: it reviews the extracted design + the doc + already-queued questions and emits ONE deduplicated, material set, and the scattered per-pass raises are suppressed. Cuts Opus cost **and** fixes the duplicate-questions bug. **Also fixes the slow, never-ending multi-round behaviour:** today every answer re-runs the FULL pipeline ([server.js:6712](agentic-sdlc-workbench/backend-node/server.js) → `processDocument` → `synthesizeDesign`), so the Opus synthesis re-sweeps the evolving design each round (~5-6 min/round) and surfaces a fresh, *independent* batch of gaps — which is why rounds feel unrelated and never converge. The clarifier should (a) **front-load** all material questions in ONE comprehensive sweep (minimize rounds), (b) be **fast/cheap** so a round isn't 5-6 min, (c) **cap rounds** (configurable / diminishing-returns stop), and (d) **decouple the expensive Opus synthesis from the question loop** — run the cheap clarifier per round, but run the Opus design-synthesis ONCE after clarifications settle (or at promote), not every round. Refines the Phase-1e clarify work; fits Phase 1.5/2. |
| #44 | **Modularize the server.js monolith** — [server.js](agentic-sdlc-workbench/backend-node/server.js) is 8,005 lines with ~167 routes and all business logic inline. Extract the change-packet materializer (700–1341), build/SN markdown assemblers, promote orchestration (6721–6989), and SN sync pipeline into `agent/materializer.js` / `agent/build-export.js` / `agent/promote.js` / `agent/sn-writeback.js`; split routes into `express.Router()` modules by group; add `getOr404`/`getProjectOr404` helpers to collapse the ~50+ duplicated fetch-by-id → 404 preambles. _(From backend review 2026-06-11.)_ |
| #45 | **Hoist re-prepared statements out of hot loops + fix design-report N+1** — ~7 sites re-compile identical SQL per row: `generateAndInsertTests` ([server.js:1134](agentic-sdlc-workbench/backend-node/server.js)), `materializeRequirementLinks` (1184), `inferLinks` (2250), `test-coverage/infer` (3959), and the design-report per-agent/per-UC `db.prepare().all()` (4290, 4828). Hoist the prepared statements above their loops (build-export at 5023 already does this). _(From backend review 2026-06-11.)_ |
| #46 | **Add missing indexes on most-joined design columns** — [schema.sql](agentic-sdlc-workbench/backend-node/schema.sql) lacks indexes on the columns every design-report/build-export joins on: `asdlc_workflow_step(workflow_id)`, `asdlc_workflow_step_cost_binding(workflow_step_id)`, `asdlc_hitl_gate(workflow_id)`, `asdlc_agent_spec(project_id)`/`(workflow_id)`, `asdlc_tool(project_id)`, `asdlc_workflow(use_case_id)`, `asdlc_agent_use_case`, `asdlc_agent_tool`, `asdlc_assist_rate_card(skill_name)`. _(From backend review 2026-06-11.)_ |
| #47 | **Harden dynamic table/column-name SQL** — several handlers interpolate a table/column name into SQL from the `RT_DESIGN`/`RT_EXPORT`/`LINK_*_META` lookup maps (e.g. `SELECT * FROM ${spec.table}`, reps [server.js:4642](agentic-sdlc-workbench/backend-node/server.js), 5151, 7840). Safe today (frozen maps keyed by validated input, values parameterized) but fragile; add a hard allowlist assertion at each entry and document that table/column names must only ever come from these maps. Same invariant as the agent-area `entity-registry` finding. _(From backend review 2026-06-11.)_ |
| #48 | **Confirmation step before project-wide standing-question fills** — when a PO answers a standing question (e.g. workflow run-volume, agent Now-Assist usage), the clarification-answer handler ([server.js:6686](agentic-sdlc-workbench/backend-node/server.js)) writes the parsed value across all *unset* workflows/agents in the project immediately. #40 made this safe (fills `IS NULL` only, validates input) and visible (audit-logged + returned as `standing_applied`), but it still auto-applies with no PO approval. Enhancement: return a *preview* ("would set runs_per_period=50 on these 8 workflows") instead of writing, show a confirm dialog in the ingest UI ([ingest.js](agentic-sdlc-workbench/frontend/modules/ingest.js)), and commit only on a second confirm request. Net-new backend preview/commit split + frontend dialog. Product decision — not a defect; revisit if PO wants a checkpoint before bulk project-wide changes. |
| #55 | **Split the design_review.js monolith** — [design_review.js](agentic-sdlc-workbench/frontend/modules/design_review.js) is 6,151 lines owning the entire Design Review feature: tab controller, 15 per-entity renderers, a hand-rolled markdown renderer + doc drawer, 9 modal editors, the exceptions/quality subsystem, CP history/detail modals, and ~1,170 lines of inline CSS. Split into `design_review_styles.js`, `dr_markdown.js`, `dr_modals.js`, `dr_quality.js`, `dr_history.js`, and one renderer module per entity-family, leaving `design_review.js` as the controller. _(From frontend review 2026-06-11.)_ |
| #56 | **Extract a shared frontend `ui.js`/`api.js`** — the modules hand-roll the same scaffolding everywhere: modal overlays (10+ sites across design_review/change_packets/testing/library, divergent class names — and cost_management mis-references design_review's private helpers, see #51); `stat()`, the `meta-grid` field loop, and token/cost formatters (copy-pasted across admin_ai/projects/cost_management/evidence/audit/validation/baseline); API-envelope normalization `Array.isArray(data) ? data : (data.items || [])` in ~10 modules; and inline-editable-cell + inline-confirm logic. Promote `openModal`, `stat`, `metaGrid`, formatters, `unwrap(data,key)`, `makeEditable`, and `inlineConfirm` into shared helpers; also splits the ~215-line `renderChangeItem` ([change_packets.js:751](agentic-sdlc-workbench/frontend/modules/change_packets.js)). _(From frontend review 2026-06-11.)_ |
| #57 | **Targeted DOM updates instead of full re-renders** — single-item edits and filter clicks rebuild whole views: [testing.js:583](agentic-sdlc-workbench/frontend/modules/testing.js) re-runs `renderTcView()` (re-fetching all TCs + scope entities) on every TC edit; the design-review quality panel rebuilds entirely on each filter-chip click ([design_review.js:5969](agentic-sdlc-workbench/frontend/modules/design_review.js)). Append/replace the affected row or toggle filter state + call the existing inner `reload()` (as `addAc` already does at testing.js:281). _(From frontend review 2026-06-11.)_ |
| #58 | **Debounce + precompute search filters** — list searches run `JSON.stringify(item).toLowerCase().includes(...)` over every row on every keystroke, then fully re-render — O(rows × payload) per character ([library.js:83](agentic-sdlc-workbench/frontend/modules/library.js), [evidence.js:77](agentic-sdlc-workbench/frontend/modules/evidence.js), [validation.js:152](agentic-sdlc-workbench/frontend/modules/validation.js)). Pre-compute a lowercased search string per row at load and debounce the input handler. _(From frontend review 2026-06-11.)_ |
| #65 | **Make ingest "fill in the blanks" by default + run the quality pass in-pipeline.** Net-new enrichment is gated behind the *Suggestive* level while uploads default to *Balanced* ([claude-processor.js:388](agentic-sdlc-workbench/backend-node/agent/claude-processor.js)); and the quality-review pass is manual-only and log-only ([quality-reviewer.js:605](agentic-sdlc-workbench/backend-node/agent/quality-reviewer.js)), never invoked by `processDocument`. Default to Suggestive (or make Balanced add net-new elements), auto-run `reviewApplication` after promote, and feed high-severity findings back. Relates to #31. _(From critical-AI-capabilities review 2026-06-11.)_ |
| #66 | **AI-author swimlane participants/paths/owners (not regex).** The entity registry has no extraction tool for workflow participants, paths, or step owners; [swimlane-deriver.js](agentic-sdlc-workbench/backend-node/agent/swimlane-deriver.js) guesses lanes with a regex categorizer and builds naive linear paths. Add extraction tools so the model authors lane structure (decisions, branches, parallel lanes) rather than relying on deterministic defaults. Relates to #36. _(From critical-AI-capabilities review 2026-06-11.)_ |
| #67 | **Resolve FR/NFR→use-case links at ingest/stage time, not promote.** Links and swimlane structure are back-filled at promote ([design-repair.js](agentic-sdlc-workbench/backend-node/agent/design-repair.js), [server.js:1360](agentic-sdlc-workbench/backend-node/server.js)), so the staged design a reviewer inspects is structurally incomplete; the orphan-relink "single-UC fallback" (design-repair.js:40,60) silently misattributes requirements in any multi-use-case project. Resolve links at stage time; replace the fallback with a clarification when attribution is ambiguous. Relates to #22. _(From critical-AI-capabilities review 2026-06-11.)_ |
| #68 | **Give the Opus synthesis pass its own higher token floor.** Synthesis (where "creation is the core job") is capped at the same default `max_tokens` as plain extraction ([ai-config.js:62](agentic-sdlc-workbench/backend-node/agent/ai-config.js), [claude-processor.js:549](agentic-sdlc-workbench/backend-node/agent/claude-processor.js)), while the SN modules raise their floor to 12000 — throttling the pass meant to emit the most net-new design. Add a dedicated synthesis max-tokens floor. Relates to #32. _(From critical-AI-capabilities review 2026-06-11.)_ |
| #69 | **Propagate design changes to dependent elements (the WF-001 drift).** When one element changes, dependents (steps, participants, paths, prose, requirements) are flagged stale with an advisory banner but never updated ([server.js:1370](agentic-sdlc-workbench/backend-node/server.js); cross-check post-apply). Auto-open a follow-up change packet proposing the dependent edits, or feed the flagged ripple targets back into the reconcile pass so dependents are actually updated. _(From critical-AI-capabilities review 2026-06-11.)_ |
| #70 | **ServiceNow push-back: decide manual handoff vs real deploy endpoint.** The outbound path emits a Markdown handoff ([server.js:5662](agentic-sdlc-workbench/backend-node/server.js)); nothing in the Workbench calls the ServiceNow SDK / Table API to write. Decide explicitly: keep a documented manual handoff (now that #59 closes the sys_id loop), or build a real deploy endpoint that shells the SDK and registers sys_ids back automatically. _(From critical-AI-capabilities review 2026-06-11.)_ |
| #71 | **Wire the now-sdk Fluent transform intake (or drop the path).** [fluent-ingest.js:15](agentic-sdlc-workbench/backend-node/agent/fluent-ingest.js) only ingests pre-existing `*.now.ts` files; the `now-sdk init/transform` that would generate them is a TODO comment, so today L2 `source_fluent` is populated from REST fields, not a real Fluent transform. Wire the transform if Fluent fidelity matters for redeploy, else drop the path so it doesn't imply it's active. _(From critical-AI-capabilities review 2026-06-11.)_ |
| #72 | **Enforce non-prod ServiceNow deploy target (prod read-only).** "Never deploy to production" is prose in the export ([server.js:5617](agentic-sdlc-workbench/backend-node/server.js)), not enforced; intake credentials aren't constrained to non-prod either. Add an explicit allowed-instance / non-prod check on the ServiceNow link + sync endpoints rather than relying on instructions. _(From critical-AI-capabilities review 2026-06-11.)_ |
| #73 | **Change-flow robustness (medium cluster).** From the change-reconciliation review: (a) no concurrency guard serializes overlapping promote/approve/sync on one project — add a per-project lock or `BEGIN IMMEDIATE`; (b) the CP-level audit + version-bump audit run *after* COMMIT ([server.js:740](agentic-sdlc-workbench/backend-node/server.js)) — move inside the transaction; (c) `auditLog` swallows all failures ([db.js:746](agentic-sdlc-workbench/backend-node/db.js)) — within a transactional apply, let an audit failure roll back; (d) the ripple corpus omits FR/NFR/business_logic/forms/data_models prose ([cross-check.js:97](agentic-sdlc-workbench/backend-node/agent/cross-check.js)) — extend it. _(From critical-AI-capabilities review 2026-06-11.)_ |
| #74 | **Ingest robustness (medium cluster).** (a) Synthesis is skipped on every clarification round >1 ([claude-processor.js:998](agentic-sdlc-workbench/backend-node/agent/claude-processor.js)) so later answers never trigger fresh inference — allow a scoped synthesis on later rounds; (b) the no-API-key stub processor is substituted silently ([processor.js:15](agentic-sdlc-workbench/backend-node/agent/processor.js)) — stamp `source:'stub'` on the result so a key-less hollow design is unmistakable; (c) hitting the extraction-loop cap finalizes a partial design with no signal ([claude-processor.js:627](agentic-sdlc-workbench/backend-node/agent/claude-processor.js)) — persist a "possibly incomplete" flag on the document. _(From critical-AI-capabilities review 2026-06-11.)_ |
| #75 | **Migrate from SQLite to PostgreSQL (multi-user + scale).** Current SQLite setup hits three hard walls at multi-user scale: (1) single writer — WAL allows concurrent reads but only one write transaction at a time, queuing every concurrent POST/PUT; (2) blocking event loop — `node:sqlite` is fully synchronous, so one slow query freezes all in-flight requests; (3) single-file, single-process — can't share the `.db` across server instances. **Scope:** ~15–22 dev-days. (a) Schema DDL port — translate `schema.sql` `datetime('now')` → `NOW()`, INTEGER booleans → `BOOLEAN`, 15 TEXT JSON blobs → `JSONB`, set up PG locally via Docker; (b) Driver + async refactor — swap `node:sqlite` for `postgres.js`, convert every `.get/.all/.run()` call in [server.js](agentic-sdlc-workbench/backend-node/server.js) (~2500 lines, 7+ transaction blocks), [db.js](agentic-sdlc-workbench/backend-node/db.js), and all 7 files in [agent/](agentic-sdlc-workbench/backend-node/agent/) to `async/await`; restructure `BEGIN/COMMIT/ROLLBACK` blocks to use a checked-out pool client; (c) SQL syntax sweep — replace 40+ `datetime('now')` in UPDATE statements, `SUBSTR/INSTR` → `SPLIT_PART`, `PRAGMA table_info()` → `information_schema.columns`; (d) Migration infrastructure — replace the embedded startup try/catch block ([db.js:27–338](agentic-sdlc-workbench/backend-node/db.js)) with node-pg-migrate numbered migrations; (e) Data migration — export existing `asdlc.db` and import to PG with type coercion. **New capabilities unlocked:** true concurrent writes (MVCC), async non-blocking queries, connection pooling, horizontal scaling, JSONB indexed queries on provenance/baseline/report blobs, full-text search on design tables, pgvector for future AI embedding search. **Full analysis in** `~/.claude/plans/i-want-an-analysis-precious-token.md`. |
| #76 | **`ASDLC_ENCRYPT_KEY` not set — per-project SN passwords stored in plaintext.** [crypto-util.js:11](agentic-sdlc-workbench/backend-node/crypto-util.js) logs a warning and falls back to storing `plain:<password>` in `asdlc_project.sn_password_enc` when `ASDLC_ENCRYPT_KEY` is absent from the environment. Set `ASDLC_ENCRYPT_KEY` to a random 32-byte secret in the backend `.env` and (optionally) add a startup assertion that aborts boot if the key is missing, so the fallback can never silently persist credentials unencrypted. _(Security — credential storage; noted 2026-06-17.)_ |
| #77 | **Confirm root `.env` and backend `.env` are gitignored.** Both files contain plaintext credentials (`SN_PASSWORD`, `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`). Verify `.gitignore` entries exist for `/.env` and `backend-node/.env` (and `.env*` wildcards), and confirm neither file is tracked in git history (`git log --all -- .env`). If either was ever committed, rotate the affected credentials immediately. _(Security — credential hygiene; noted 2026-06-17.)_ |
| #80 | **Phase 4 — Generic SN artifact fidelity + editor UI (follows Phases 1–3, Dev branch).** Two parts. (a) **Typed Fluent field-maps (Tier-B curation):** today every generic artifact deploys via the reliable generic `Record({table,data})` fallback ([buildSNDeltaMarkdown, server.js](agentic-sdlc-workbench/backend-node/server.js)) regardless of `deploy_strategy` — faithful (raw columns pass through) but not idiomatic. Curate per-type field schemas in `asdlc_sn_type_registry.field_schema` so `deploy_strategy='typed'` artifacts emit first-class native Fluent (`new Acl({...})`, `new Table({...})` with `Column` children, `new Sla({...})`) instead of `Record()`. This is fidelity/idiom polish, NOT a data-loss fix. (b) **Schema-driven editor UI** — new `frontend/modules/sn_artifacts.js`: Tier-A artifacts edit via the existing L1 editors; Tier-B render a form from the registry `field_schema`; Tier-C a raw key/value editor + an `override_fields` "advanced (bypasses typed API)" panel; Level-2 provenance (`source_sys_id`/`source_fluent`/etc.) behind a "Technical details" disclosure. Generic artifacts are captured, stored, round-tripped, and Fluent-emitted today (Phases 1–3) but have no dedicated viewing/editing surface. _(SN generic-artifact program, Phase 4. Phase 5 = automate the deploy step itself, tracked by #70/#71.)_ |
| ~~#81~~ | **✅ DONE 2026-07-07 — `wipe-project.js` clears the SN substrate.** The reset script ([wipe-project.js](agentic-sdlc-workbench/backend-node/wipe-project.js)) now deletes `asdlc_sn_artifact` + `asdlc_sn_change_signal` for the project and nulls the `sn_artifact_id` back-links on the 5 Level-1 tables (`asdlc_data_model`/`form_design`/`business_logic`/`catalog_item`/`integration`); `asdlc_sn_type_registry` left untouched (global SDK snapshot). Enables clean slice re-ingest (#98) without orphaning/duplicating generic artifacts or carrying stale change-signal baselines. _(SN generic-artifact program; housekeeping.)_ |
| #82 | **Update the `sqlite-audit` skill's `phase3_checks.mjs` for the new SN tables.** Per the skill's standing maintenance rule (update its checks whenever `asdlc.db` schema changes), add the generic substrate: tables `asdlc_sn_artifact` + `asdlc_sn_type_registry`, the new polymorphic `entity_type='sn_artifact'` value on `asdlc_change_packet_item`, the `sn_artifact_id` FK column on the 5 L1 tables (`asdlc_data_model`/`form_design`/`business_logic`/`catalog_item`/`integration`), the `asdlc_sn_change_signal` table (#86b — per-(project, sys_id) ServiceNow modification counter + audit fields), and the `asdlc_project.sn_import_profile_json` column (#98 — per-project slice import profile). The skill is **not installed in this workspace**, so this must be done wherever the skill lives. _(SN generic-artifact program; housekeeping.)_ |
| #83 | **Auto-refresh curated `field_schema` into existing registries on boot (Phase 4a follow-up).** New typed-Fluent recipes (`asdlc_sn_type_registry.field_schema`, e.g. Property/Role from Phase 4a, commit `d3b23c9`) only reach a database via the checked-in baseline at **seed time** — and `seedFromBaseline()` no-ops once the registry is populated ([sn-capability-sync.js](agentic-sdlc-workbench/backend-node/agent/sn-capability-sync.js)). So an **existing** DB keeps exporting those types via the generic `Record()` fallback until someone manually runs `syncCapabilities()`; only **fresh** DBs pick up new recipes automatically. Not data-loss — newly-added recipes just don't take effect until a refresh. **Fix:** on boot, reconcile the curated overlay (the `CURATED.field_schema` entries) into the live registry even when populated — e.g. an idempotent startup pass that upserts curated rows from the baseline/overlay (the upsert already `COALESCE`s so SDK-only rescans are preserved), or auto-run a lightweight curated-only refresh. Keep it cheap and non-destructive. _(SN generic-artifact program, Phase 4a follow-up; noted 2026-06-23.)_ |
| #78 | **Phase 2: DB-backed prompt templates + Admin editor + per-role A/B (builds on prompt externalization).** Phase 1 (2026-06-16) moved the ingest prompt prose out of hardcoded JS string arrays into versioned files under [agent/prompts/](agentic-sdlc-workbench/backend-node/agent/prompts/) (`extraction/`, `reconcile/`, `synthesis/`), rendered by [prompt-templates.js](agentic-sdlc-workbench/backend-node/agent/prompt-templates.js) with `{{var}}` substitution + load-once cache + graceful fallback; git now gives prompt versioning/diff/rollback, and the assembly logic stayed in `claude-processor.js`. Phase 2: promote these to a DB-backed `asdlc_prompt_template` table (version rows, `is_active`, per-role/section scoping, author/timestamp) + an Admin editor so prompts can be edited **live without a redeploy** and **A/B-tested per role** (extraction / reconcile / synthesis). `render()` resolution becomes DB-active-version → file → empty, preserving graceful degradation; add a byte-parity guard when seeding the table from the files. _(Ingestion-quality program; deferred from the 2026-06-16 borrow-the-ideas refactor.)_ |
| #92 | **`npm test` is still the unmodified stub despite 20 real, working test suites.** `package.json`'s `"test"` script is `echo "Error: no test specified" && exit 1` — every `test-*.js` file at `backend-node/` root (20 of them, all deterministic/stub-mode-safe: forced `ANTHROPIC_API_KEY=''`, isolated temp DB, randomized port, mocked SN fetches) has to be run one file at a time by hand. Wire an aggregate script (e.g. `for f in test-*.js; do node $f || exit 1; done`) into `"test"`. Also: there is no `devDependencies` block at all — no linter, no declared test framework — add one (even a minimal ESLint config) so style/obvious breakage is caught before commit. This is the prerequisite for #94 (CI/CD). _(Tech-debt audit 2026-07-06.)_ |
| #93 | **No automated DB backup scheduling + no `.env.example`.** `backend-node/backup-db.js` exists and works (manual `VACUUM INTO`, timestamped snapshots) but nothing schedules it — the newest backup on disk at audit time was already ~4 weeks old on a live app with real project data. Wire it into Task Scheduler/cron. Separately, ~16 distinct env vars are read directly via `process.env.*` across 44 files, and only 3 are documented anywhere — add a checked-in `.env.example` (no secrets) listing all of them so a new developer doesn't have to reverse-engineer the list from source. _(Tech-debt audit 2026-07-06.)_ |
| #94 | **No CI/CD pipeline.** No `.github/workflows/` or equivalent anywhere in the repo — nothing gates a broken commit before it reaches `main`. Depends on #92 (an aggregate test script must exist first). Once that lands, a baseline GitHub Actions workflow running lint + the full test suite on every push/PR is a ~1-day task, not weeks. `docs/PRODUCTION_WORKPLAN.md` already lists this as unstarted roadmap; this item makes it actionable now that the prerequisite is scoped. _(Tech-debt audit 2026-07-06.)_ |
| #95 | **Retry / resume the failed items of a partially-applied Change Packet.** When a CP applies but one or more items fail (e.g. a `delete` blocked by a still-present dependent — "workflow has 1 dependent agent_spec — not deleted"), the CP is left in a partially-applied state with no in-app way to re-run just the failed items: the successful items are already marked applied, the CP is `approved`, and there is no "resume" action. Today this requires a manual DB repair (as done for the WF-001/WF-002 merge on 2026-07-06). Add an endpoint + UI action to re-attempt only the un-applied items of an approved CP — now safe to do since the reparent-on-update fix (mtUpdate re-resolves parent links) means a reattach-then-delete merge will apply cleanly on a second pass. Should re-run through the normal apply path (audited, dependency-guarded), report per-item outcomes, and mark the CP fully applied only when no items remain blocked. _(Surfaced 2026-07-06 while applying the ingest-quality-findings CP; see PR #1 / commit 94f731e.)_ |
| #96 | **Agent "where-used" view on the Agent Design Report — workflows, call frequency, cross-application reuse.** The agent detail card ([design_review.js:1259](agentic-sdlc-workbench/frontend/modules/design_review.js)) shows only linked use cases + the single `agent.workflow` in its breadcrumb, and the printable report's `renderAgents` ([report-html.js:661](agentic-sdlc-workbench/backend-node/report-html.js)) lists linked use cases + tool bindings but nothing about workflow membership or invocation volume. Add a "Where Used" sub-section covering three things: **(1) Workflows this agent is part of** — an agent participates in a workflow via `asdlc_workflow_participant.agent_spec_id` → `asdlc_workflow_step.owner_participant_id` → `asdlc_workflow`; list each workflow (with the steps it owns) and deep-link to it. **(2) Frequency called per period** — derive from the workflows it participates in: `SUM(asdlc_workflow.runs_per_period)` across those workflows (the same participant→step→workflow join already used by `agentCostStmt` at [report-html.js:309](agentic-sdlc-workbench/backend-node/report-html.js)), shown per planning period + annualized. **(3) Cross-application reuse** — agents are project-scoped (`asdlc_agent_spec.project_id`), so "is this agent used in another application's workflow" needs a match across projects by normalized name/slug; surface any other `asdlc_project` whose design contains a same-identity agent bound to a workflow, as a "also used in: <app>" note. Backend: extend the agent design-report query ([server.js:4883](agentic-sdlc-workbench/backend-node/server.js)) + `gatherData` agents block. Frontend: new sub-section in the agent renderer with deep-links (reuse `setDrillDown(scope, dr-entity-<id>)` + `navigate()`, per [change_packets.js:888](agentic-sdlc-workbench/frontend/modules/change_packets.js)). Relates to #21 (per-app attribution). _(User request 2026-07-07.)_ |
| #97 | **Make "📄 Full Design Report" reflect everything a human should review + deep-link each entity back into the Workbench.** The report button ([design_review.js:360](agentic-sdlc-workbench/frontend/modules/design_review.js)) opens the standalone HTML built by [report-html.js](agentic-sdlc-workbench/backend-node/report-html.js), whose TOC ([report-html.js:446](agentic-sdlc-workbench/backend-node/report-html.js)) currently covers only: Exec Summary, Use Cases, Workflows (steps + HITL gates), Agents, Tools, Data Models, Catalog Items, Integrations, Requirements (FR/NFR), Test Cases, AI Costs, Guardrails, Governance, Data Sources. **Reviewable design content that exists in the repo but is missing from the report:** (a) **User Stories** (`asdlc_user_story`; a `design-report/user-stories` endpoint already exists at [server.js:5342](agentic-sdlc-workbench/backend-node/server.js)); (b) **Acceptance criteria + dependencies** on FRs — both are `safeJson`'d in `gatherData` ([report-html.js:283](agentic-sdlc-workbench/backend-node/report-html.js)) but `renderRequirements` ([report-html.js:976](agentic-sdlc-workbench/backend-node/report-html.js)) never renders them; (c) **Workflow participants / swimlanes / paths / RASIC** — the report shows steps + HITL gates only, not the participant lanes, branch paths, or the RASIC matrix (relates to #36); (d) **Requirement → test-case traceability / coverage**; (e) **Generic ServiceNow artifacts** (`asdlc_sn_artifact`, Tiers A/B/C) — captured & round-tripped but absent from the report; (f) **Open exceptions + quality-review findings + data-issue flags** — the SPA already fetches open exceptions in `loadReport` ([design_review.js:531](agentic-sdlc-workbench/frontend/modules/design_review.js)); surface them so a reviewer sees unresolved issues in the printed artifact; (g) **✨ AI-suggested provenance** on system-generated rows (relates to #26); (h) **Change-Packet history / audit trail** and **baselines** for the application. Audit `RT_DESIGN`/`design-report/*` scopes ([server.js:5410](agentic-sdlc-workbench/backend-node/server.js)) against the report sections and close every gap so the printable report is a true superset of what's reviewable in the Design Review tab. **Plus deep-links:** each entity/section in the report should link back to its exact spot in the Design Workbench — the SPA already supports this via `setDrillDown(scope, dr-entity-<id>)` + `navigate('design_review')` ([change_packets.js:888](agentic-sdlc-workbench/frontend/modules/change_packets.js)) and per-entity anchors `dr-entity-<id>` ([design_review.js:888](agentic-sdlc-workbench/frontend/modules/design_review.js)); emit an absolute app URL per card that drives the same drill-down so a reviewer can jump from the report straight to the editable record. _(User request 2026-07-07.)_ |
| #100 | **R7 follow-on — full WB-field-space 3-way merge for RICH (Tier-A) artifacts.** #99 delivered the field-level merge for *generic* artifacts (Workbench payload = ServiceNow payload → a sound 3-way) and, for *rich* artifacts, the deterministic SN-side field delta as reconciler context with the record-level both-side floor as the safe fallback. A true per-field merge for rich records is blocked by an impedance mismatch: the stored base (`source_fluent`) is in ServiceNow-field space, while a rich canonical row is in Workbench-design space, so there is no WB-side base to diff a WB field against. To close it, store the last-synced *inferred* (Workbench-shaped) design as a per-record base snapshot at apply time, then run `classifyFields` in WB-design space so rich records also route field-by-field (auto-apply SN-only fields on a WB-edited record, keep WB-only fields, escalate only both-changed). Larger change (new base storage + reconcile rewiring). _(Round-trip fidelity assessment, gap G2/R7 — remaining slice after #99.)_ |
| #89 | **Consolidate the shared AI client (`getClient` / `hasKey` / JSON-extractor) into one module.** The same Anthropic-client construction, `hasKey()` API-key guard, and JSON-extraction helper are re-implemented across the agent modules — e.g. [cross-check.js](agentic-sdlc-workbench/backend-node/agent/cross-check.js), [sn-review.js](agentic-sdlc-workbench/backend-node/agent/sn-review.js), [sn-reconcile.js](agentic-sdlc-workbench/backend-node/agent/sn-reconcile.js), [rasic-deriver.js](agentic-sdlc-workbench/backend-node/agent/rasic-deriver.js), [agent-prompt-drafter.js](agentic-sdlc-workbench/backend-node/agent/agent-prompt-drafter.js), [requirement-linker.js](agentic-sdlc-workbench/backend-node/agent/requirement-linker.js). Extract a single `agent/ai-client.js` exposing `getClient()`, `hasKey()`, and `extractJson()`, and refactor the agent modules to import it — one place to fix client config, key handling, and brittle JSON parsing. Overlaps refactor themes #35 (shared clarifier/dedup) and #44 (server split). _(Surfaced by graphify knowledge-graph run 2026-07-06 — the only flagged item not already tracked; see `graphify-out/`.)_ |
| #101 | **[PARTLY ADDRESSED 2026-07-08 by #103]** The dominant cost driver is gone for most types: catalog_item / data_model / form_design / integration / business_logic (≈the whole `SN_CATALOG` volume, incl. the ~60% that is business logic) now capture deterministically with **zero** Opus. The levers below remain relevant ONLY for the residual AI types (agent_spec / use_case / tool — interpretive — and header-only workflow), which are low-volume. **ServiceNow reverse-engineer is single-record, sequential, high-effort Opus — a full record-type import is hours, not minutes.** Measured live: 182 records / 28.3 min / $8.81 (~9.3 sec, ~$0.05 per record) — extrapolated, a full uncapped `sys_script` (5,740 records) import would run ~14.8 hours / ~$278. Root causes, all confirmed in code: (a) `resolveModel('reverse_engineer')` defaults to `claude-opus-4-8`, the slowest/priciest tier ([ai-config.js](agentic-sdlc-workbench/backend-node/agent/ai-config.js)); (b) extended thinking is ON by default at "high" effort with an 8,000-token budget for this role ([ai-config.js:115-116](agentic-sdlc-workbench/backend-node/agent/ai-config.js)) — likely the single largest contributor, since it's pure latency the user never sees; (c) `reverseEngineer()` is a plain sequential `for` loop with **zero concurrency** ([sn-reverse-engineer.js:268-270](agentic-sdlc-workbench/backend-node/agent/sn-reverse-engineer.js)) — one record fully completes before the next starts. **Levers, cheapest first:** (1) test lowering/disabling thinking effort and/or switching to a faster model for the `reverse_engineer` role via the existing per-role admin settings — zero code, testable today on a small batch; (2) add bounded concurrency (5–10 in flight) to the reverse-engineer loop — doesn't change per-call latency but multiplies wall-clock throughput directly, e.g. ~8x concurrency turns a 14.8-hour import into ~1.9 hours at the same cost/quality; (3) longer-term, batch multiple records into one prompt/tool-call to amortize per-call overhead — bigger redesign, needs care around per-record attribution and quality. _(User request 2026-07-08, after stopping a live dry run.)_ |
| ~~#103~~ | **✅ DONE 2026-07-08 — Deterministic direct-map + on-request narration built.** New [sn-direct-map.js](agentic-sdlc-workbench/backend-node/agent/sn-direct-map.js) maps catalog_item / data_model / form_design / rest_message / connection_alias straight from raw capture into the extract-tool `entity_data` shape (reusing `buildInferred`), and business_logic to name + logic_type with a BLANK narrative. `reverseEngineerOne` short-circuits these (zero tokens); `runSyncPlan` routes changed deterministic records through a deterministic proposal + the existing `gateProposal` floors (no Opus reconcile/review); materiality no longer suppresses business logic (a design entry per rule). On-request narration via `POST /projects/:id/design/business-logic/:id/explain` + an "✨ Explain with AI" button ([design_review.js](agentic-sdlc-workbench/frontend/modules/design_review.js)). Capture expanded for faithful copies (catalog category + fulfillment workflow + joined variables; table columns joined). Only agent_spec/use_case/tool (interpretive) + header-only workflow still call Opus. Change detection / conflict resolution / round-trip identity unchanged (they never used AI). Tests: `test-sn-direct-map.js` (26), `test-materiality.js` updated. Config/flat types remain already-generic (out of scope; live-reachability check still open). _(Research below; built 2026-07-08.)_ **[Original research:]** Read all 20 SN-reachable `extract_*` tool schemas in [entity-registry.js](agentic-sdlc-workbench/backend-node/agent/entity-registry.js) and classified each by how much of its schema is literal ServiceNow data vs. genuine narrative interpretation. **Strong bypass candidates (near-100% literal fields):** `catalog_category` (sc_category), `choice_set` (sys_choice), `variable_set` (item_option_new_set), `user_group` (sys_user_group), `sla_definition` (contract_sla), `email_notification` (sysevent_email_action), `inbound_rest_api` (sys_ws_definition), `service_portal`/`workspace` (sp_portal/sys_ux_page_registry), `rest_message`/`connection_alias` (sys_rest_message/sys_alias), `dashboard`/`report`/`kpi` (par_dashboard/sys_report/pa_indicator) — each has at most one soft "purpose"-style field, everything else is a direct copy. **Note:** these route through `SN_TABLE_TO_TYPE` in [sn-reverse-engineer.js:38-52](agentic-sdlc-workbench/backend-node/agent/sn-reverse-engineer.js), but none appear in `SN_CATALOG`/`SN_SURFACES` ([sn-catalog.js](agentic-sdlc-workbench/backend-node/agent/sn-catalog.js)) — whether they're live-reachable via the current SN sync capture path (vs. only the forward/document-ingestion direction) needs a quick empirical check before investing here. **Hybrid (mostly literal + one genuinely useful narrative field):** `data_model` (sys_db_object — only `purpose`/field `meaning` are soft); `form_design` (sys_ui_form/sys_ui_policy — only `behavior_notes` is soft, and — mirroring #102 — `sys_ui_policy`'s capture only feeds `short_description`+`table`, not the actual condition, so the model is asked for behavior it was never shown here too). **The single biggest lever, found in code comments, not guessed:** `business_logic` (`sys_script`/`sys_script_client`/`sys_script_include`/`sys_ui_action` — 14,748 of the user's 24,680-record global scope, ~60%) is explicitly labeled in [sn-catalog.js:38-51](agentic-sdlc-workbench/backend-node/agent/sn-catalog.js) as **"Tier C — implementation code... captured for drift, NOT design intent."** The schema here (`plain_english`/`when_runs`/`conditions`) genuinely requires reading the real script body (which IS fed to the model, unlike catalog items) — so this isn't a data-starvation problem like #102, it's a "should we even be authoring a Level-1 narrative for something the system's own intent says isn't design intent" problem. Recommend: skip full Opus reverse-engineering by default for **new** business-logic records (deterministic hash+metadata capture already covers drift detection, which is the stated goal), reserving AI narration for an explicit human-triggered "explain this rule" action. **Keep AI as-is (genuinely interpretive, low volume):** `agent_spec`/`use_case`/`tool` (Now Assist) — asks for goals, risks, orchestration strategy; real inference, not a copy job, and only ~10 records in the user's instance anyway. _(User research request 2026-07-08.)_ | Read all 20 SN-reachable `extract_*` tool schemas in [entity-registry.js](agentic-sdlc-workbench/backend-node/agent/entity-registry.js) and classified each by how much of its schema is literal ServiceNow data vs. genuine narrative interpretation. **Strong bypass candidates (near-100% literal fields):** `catalog_category` (sc_category), `choice_set` (sys_choice), `variable_set` (item_option_new_set), `user_group` (sys_user_group), `sla_definition` (contract_sla), `email_notification` (sysevent_email_action), `inbound_rest_api` (sys_ws_definition), `service_portal`/`workspace` (sp_portal/sys_ux_page_registry), `rest_message`/`connection_alias` (sys_rest_message/sys_alias), `dashboard`/`report`/`kpi` (par_dashboard/sys_report/pa_indicator) — each has at most one soft "purpose"-style field, everything else is a direct copy. **Note:** these route through `SN_TABLE_TO_TYPE` in [sn-reverse-engineer.js:38-52](agentic-sdlc-workbench/backend-node/agent/sn-reverse-engineer.js), but none appear in `SN_CATALOG`/`SN_SURFACES` ([sn-catalog.js](agentic-sdlc-workbench/backend-node/agent/sn-catalog.js)) — whether they're live-reachable via the current SN sync capture path (vs. only the forward/document-ingestion direction) needs a quick empirical check before investing here. **Hybrid (mostly literal + one genuinely useful narrative field):** `data_model` (sys_db_object — only `purpose`/field `meaning` are soft); `form_design` (sys_ui_form/sys_ui_policy — only `behavior_notes` is soft, and — mirroring #102 — `sys_ui_policy`'s capture only feeds `short_description`+`table`, not the actual condition, so the model is asked for behavior it was never shown here too). **The single biggest lever, found in code comments, not guessed:** `business_logic` (`sys_script`/`sys_script_client`/`sys_script_include`/`sys_ui_action` — 14,748 of the user's 24,680-record global scope, ~60%) is explicitly labeled in [sn-catalog.js:38-51](agentic-sdlc-workbench/backend-node/agent/sn-catalog.js) as **"Tier C — implementation code... captured for drift, NOT design intent."** The schema here (`plain_english`/`when_runs`/`conditions`) genuinely requires reading the real script body (which IS fed to the model, unlike catalog items) — so this isn't a data-starvation problem like #102, it's a "should we even be authoring a Level-1 narrative for something the system's own intent says isn't design intent" problem. Recommend: skip full Opus reverse-engineering by default for **new** business-logic records (deterministic hash+metadata capture already covers drift detection, which is the stated goal), reserving AI narration for an explicit human-triggered "explain this rule" action. **Keep AI as-is (genuinely interpretive, low volume):** `agent_spec`/`use_case`/`tool` (Now Assist) — asks for goals, risks, orchestration strategy; real inference, not a copy job, and only ~10 records in the user's instance anyway. _(User research request 2026-07-08.)_ |
| ~~#102~~ | **✅ DONE 2026-07-08 (folded into #103's build) — catalog items now direct-mapped, no AI.** `sc_cat_item` capture expanded to grab `category` + fulfillment `workflow`, and the already-captured `item_option_new` variables are joined into the L1 `variables` column; the record materializes deterministically with zero AI and no more low-confidence guessing. See #103. **[Original:]** Reverse-engineer over-invokes AI for record types whose extraction schema is already almost entirely literal fields — catalog items are the clearest case.** Traced live: `sc_cat_item`'s capture only feeds the model `name` + `short_description` ([sn-catalog.js:58-59](agentic-sdlc-workbench/backend-node/agent/sn-catalog.js)) — no `category`, no workflow reference, no variables — yet `extract_catalog_item`'s tool schema explicitly asks the model to report `category`, `workflow_name`, and the full `variables` array ([entity-registry.js:788-824](agentic-sdlc-workbench/backend-node/agent/entity-registry.js)). The model is being asked for fields it was never shown, and (correctly, per its system prompt) reports low confidence rather than confabulate — that's most of what drove the 30–62% confidence seen on a real catalog-item dry run. Worse, `extract_catalog_item`'s properties (name, short_description, category, workflow_name, variables, who_can_order, delivery_time) are **all literal ServiceNow data** — unlike business-logic/workflow extraction, there is no narrative "purpose"/"behavior" field requiring genuine interpretation. Catalog items (and likely other structurally-simple types) are strong candidates for a **deterministic direct-mapping fast path**: capture `category` + the workflow reference field directly, join the already-separately-captured `item_option_new` variables into the same record, and skip the AI call entirely for types where the target schema is provably field-for-field mappable — reserving Opus reverse-engineering for types that actually require semantic interpretation (business rules, workflows). Closes most of #101's cost/time problem for these types as a side effect, without touching quality. _(User request 2026-07-08.)_ |
| #104 | **Business-logic write-back drops the executable script — the L1 deploy path can't recreate a rule.** Surfaced while building #103. A `business_logic` record's real content is its script, stored in `source_fluent`; but the outbound export strips `source_fluent` (`STRIP_INTERNAL` + `stripProvenance`, [server.js](agentic-sdlc-workbench/backend-node/server.js)) and `asdlc_business_logic` has no `script` column — so a PATCH/POST for a business rule emits only name/logic_type/(now-blank narrative), never the script. A reverse-engineered rule therefore cannot be faithfully redeployed to ServiceNow from its L1 record today; the only faithful raw-replay path is the generic `sn_artifact` `Record({table,data:payload})` substrate. **Fix options:** either route business logic's raw script to a deployable generic artifact (so `Record()` redeploys it verbatim), or add a deploy-time step that re-attaches the script from `source_fluent`. Pre-existing (not introduced by #103), but #103 makes it explicit since business logic no longer even carries a narrative. Relates to #70/#71 (deploy path). _(Discovered 2026-07-08 during the deterministic-capture build.)_ |
